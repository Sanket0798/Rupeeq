import { GenericHeroWrapper } from '../shared';
import { DEBT_CONSOLIDATION_HERO } from '../../constants';

const DebtConsolidationHero = () => {
  return <GenericHeroWrapper {...DEBT_CONSOLIDATION_HERO} />;
};

export default DebtConsolidationHero;
