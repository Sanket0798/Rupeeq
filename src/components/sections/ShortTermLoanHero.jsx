import { GenericHeroWrapper } from '../shared';
import { SHORT_TERM_LOAN_HERO } from '../../constants';

const ShortTermLoanHero = () => {
  return <GenericHeroWrapper {...SHORT_TERM_LOAN_HERO} />;
};

export default ShortTermLoanHero;
